/**
 * Returns the last item in a list
 * @param list list of items
 * @returns any - the first item in list
 */
function lastItemOfList(list: any[]): any {
  return list[list.length - 1];
}

export default lastItemOfList;
