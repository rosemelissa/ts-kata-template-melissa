/**
 * Returns the first item in a list
 * @param list list of items
 * @returns any - the first item in list
 */
function firstItemOfList(list: any[]): any {
  return list[0];
}

export default firstItemOfList;
